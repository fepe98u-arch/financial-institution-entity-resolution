# Phase 1 dependencies only.
# Later phases will add: sqlalchemy, psycopg (PostgreSQL driver), rapidfuzz,
# sentence-transformers, scikit-learn, pytest — added when each phase actually needs them.

streamlit==1.38.0
polars==1.9.0
openpyxl==3.1.5
PyYAML==6.0.2
python-dotenv==1.0.1
